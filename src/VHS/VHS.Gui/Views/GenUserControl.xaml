<UserControl x:Class="VHS.Gui.Views.GenUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VHS.Gui.Views"
             d:DesignHeight="{Binding Height}" d:DesignWidth="{Binding Width}"
             d:DataContext="{Binding DataContext}">
    
    <Grid>
        <TextBlock Text="Hey there from VHS"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"/>
    </Grid>
</UserControl>
